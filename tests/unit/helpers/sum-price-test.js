import { sumPrice } from 'lesson-03/helpers/sum-price';
import { module, test } from 'qunit';

module('Unit | Helper | sum price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sumPrice([42]);
  assert.ok(result);
});
